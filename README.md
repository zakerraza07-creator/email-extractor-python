# 📧 Email Extractor & Analyzer (Python)

This is a Python mini project that reads data from a text file and extracts valid email addresses using Regular Expressions (Regex).  
It also demonstrates the use of map(), filter(), and reduce() functions.

## 🚀 Features
- Reads text data from a file
- Extracts email addresses using regex
- Converts emails to lowercase
- Filters only Gmail IDs
- Counts total valid emails
- Writes filtered emails into a new file

## 🛠 Concepts Used
- File Handling
- Regular Expressions (re module)
- map(), filter(), reduce()
- Lambda functions
- Functional Programming in Python

## 📂 Input File
data.txt  
(contains raw text with email addresses)

## 📂 Output File
data2.txt  
(contains only valid Gmail addresses)

## ▶ How to Run
1. Clone the repository  
2. Put email data in `data.txt`  
3. Run the script:
